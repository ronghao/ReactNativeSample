# ReactNativeSample
ReactNative实例

# 背景
+ windows环境

# 步骤
+ 安装必备软件
    + Python2
    + jdk
    + android studio
    + Node
+ 安装reactNative
    + `npm install -g react-native-cli`
+ 创建项目
    + `react-native init ReactNativeProjectProject`
+ 启动项目
    + `cd AwesomeProject`
    + `npm start` 